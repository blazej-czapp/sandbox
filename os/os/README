You can run the OS using Bochs emulator (http://bochs.sourceforge.net/). Simply run `bochs` in the directory where bochsrc and os-image are located. To install Bochs on ubuntu do:

sudo apt-get install bochs bochs-x